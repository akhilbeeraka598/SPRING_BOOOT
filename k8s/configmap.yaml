apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  db-host: "mysql-service" # Assuming a MySQL service exists in the cluster
  db-port: "3306"
  db-name: "mydb"
  db-user: "root"
  api-base-url: "http://backend-service:8080/api"
